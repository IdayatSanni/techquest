doctype html
html
  head
    meta(charset="utf-8")
    title #{title} | Test website
    
    block scripts 
      link(rel="stylesheet", type="text/css", href="/css/style.css")
      link(rel="stylesheet" href="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.css")
    
  body
    include includes/header.pug
    header#header 
      .header-content
    main#main 
      block main-content 

    footer#footer 
      p &copy; Copyright HTTP5211, 2023.
    
    
    script(src="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.js")
    
    script.
      window.easyQuizzes = !{JSON.stringify(easyQuizzes)};
      window.mediumQuizzes = !{JSON.stringify(mediumQuizzes)};
      window.hardQuizzes = !{JSON.stringify(hardQuizzes)};
      window.easyJQuizzes = !{JSON.stringify(easyJQuizzes)};
      window.mediumJQuizzes = !{JSON.stringify(mediumJQuizzes)};
      window.hardJQuizzes = !{JSON.stringify(hardJQuizzes)};
    
    script(src="/js/slider.js")
    
      
      
